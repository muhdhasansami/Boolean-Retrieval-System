# Boolean-Retrieval-System
A Python-based Boolean Retrieval System built on a collection of speeches by Donald Trump. This project implements both inverted indexing and positional indexing to enable efficient keyword search, Boolean queries (AND, OR, NOT), and proximity queries.  
